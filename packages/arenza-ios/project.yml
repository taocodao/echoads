name: Arenza
options:
  bundleIdPrefix: com.cmxs
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true
  defaultConfig: Debug

configs:
  Debug: debug
  Release: release

settings:
  base:
    SWIFT_VERSION: "5.10"
    IPHONEOS_DEPLOYMENT_TARGET: "18.0"
    ENABLE_BITCODE: NO
    ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: YES
    CODE_SIGN_STYLE: Automatic
    SWIFT_STRICT_CONCURRENCY: targeted
  configs:
    Release:
      CODE_SIGN_STYLE: Manual
      DEVELOPMENT_TEAM: "5R8WY2Q734"
      CODE_SIGN_IDENTITY: "Apple Distribution"
      PROVISIONING_PROFILE_SPECIFIER: "match AppStore com.cmxs.arenza"

targets:
  Arenza:
    type: application
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: Arenza
        excludes:
          - "**/.DS_Store"
    info:
      path: Arenza/Resources/Info.plist
      properties:
        CFBundleDisplayName: Arenza
        CFBundleName: Arenza
        CFBundleIdentifier: com.cmxs.arenza
        CFBundleVersion: "1"
        CFBundleShortVersionString: "0.1.0"
        UILaunchScreen:
          UIColorName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIRequiresFullScreen: false
        NSFaceIDUsageDescription: "Arenza uses Face ID to protect your PoD signing key and verify ad impressions."
        NSUserTrackingUsageDescription: "Arenza uses your advertising identifier to deliver relevant sports ads. You can deny this — a privacy-preserving anonymous ID will be used instead."
        UIBackgroundModes:
          - audio
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true   # Dev only — restrict to cmxs.io in production
    entitlements:
      path: Arenza/Arenza.entitlements
      properties:
        keychain-access-groups:
          - $(AppIdentifierPrefix)com.cmxs.arenza
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.cmxs.arenza
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_FILE: Arenza/Resources/Info.plist
      configs:
        Debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
          DEBUG_INFORMATION_FORMAT: dwarf
          ONLY_ACTIVE_ARCH: YES
        Release:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: ""
          DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
          ONLY_ACTIVE_ARCH: NO
          CODE_SIGN_STYLE: Manual
          DEVELOPMENT_TEAM: "5R8WY2Q734"
          CODE_SIGN_IDENTITY: "Apple Distribution"
          PROVISIONING_PROFILE_SPECIFIER: "match AppStore com.cmxs.arenza"
    dependencies: []
    scheme:
      testTargets:
        - ArenzaTests
      environmentVariables:
        CMXS_API_BASE: http://localhost:3001
        CMXS_TOKEN_ADDRESS: "0xbD8A3FDd326C08F260f5ef77A7c79ec4B5091AeC"
        ORACLE_ADDRESS: "0x9a45Ea9b31eF2E2c36f9478874d0805446804cC1"
        AD_BURN_ADDRESS: "0xFF5c1cF5f22689517aB2950101B0a623d5c6884D"
        BASE_SEPOLIA_CHAIN_ID: "84532"

  ArenzaTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: ArenzaTests
    dependencies:
      - target: Arenza
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.cmxs.arenza.tests

schemes:
  Arenza:
    build:
      targets:
        Arenza: all
    run:
      config: Debug
      environmentVariables:
        CMXS_API_BASE: http://localhost:3001
    test:
      config: Debug
      targets:
        - name: ArenzaTests
          parallelizable: true
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
